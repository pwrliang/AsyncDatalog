CHECK_INTERVAL = 1500
CHECK_TYPE = DELTA
CHECK_COND = E
CHECK_THRESHOLD = 0
DYNAMIC = FALSE
THREAD_NUM = 32
DEBUGGING = FALSE
MESSAGE_TABLE_INIT_SIZE = 10000
MESSAGE_TABLE_UPDATE_THRESHOLD = 0
MESSAGE_TABLE_WAITING_INTERVAL = 5000
RULE:
edge(int src:0..4847570, (int dst, int weight)).
sssp(int node:0..4847570, int dist).

edge(s, t, w) :- l=$read("/home/gengl/Datasets/SSSP/LiveJournal/edge.txt"), (s1,s2,s3)=$split(l, "	"),
             s=$toInt(s1), t=$toInt(s2), w=$toInt(s3).

sssp(s, d) :- l=$read("/home/gengl/Datasets/SSSP/LiveJournal/node.txt"), (s1,s2)=$split(l, "	"), s=$toInt(s1), d=$toInt(s2).
sssp(Y, $dmin(D)) :- sssp(X, D1), edge(X, Y, D2), D = D1 + D2.

#/home/gengl/socialite-before-yarn/examples
#E:\\socialite-before-yarn\\examples\\
#/home/gengl/Datasets/nodes/web-Google_fix_nodes.txt
#/home/gengl/Datasets/weight/web-Google_fix_weight.txt
#dateset        node
#    livejournal    4847571
#    google         875713
#    berkstan       685230
# NOTE MESSAGE_TABLE_UPDATE_THRESHOLD FOR SSSP CANNOT TOO LOW